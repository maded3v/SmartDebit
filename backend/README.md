# Backend

Здесь хранится рабочая структура базы данных проекта SmartDebit.

- `db/schema.sql` - основная схема таблиц и индексов.
- `db/README.md` - пояснения по расхождениям с диаграммой.

Для локальной разработки mock-api использует эту схему и создает sqlite-файл в `backend/data/`.
